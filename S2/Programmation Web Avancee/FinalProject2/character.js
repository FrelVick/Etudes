/**
 * Created by Виктор on 10.04.2017.
 */
/*class Character extends Body {
    constructor(img, ctx, size, v, w, h) {
        super(v, w, h, true);
        this.img = img;
        this.ctx = ctx;
        this.size = size;
        this.frame = 0;             //frame of move image
        this.type = 0;              //type of character
    }

    draw(offset) {
        this.ctx.clearRect(0, 0, this.ctx.canvas.width, this.ctx.canvas.height);
        this.ctx.drawImage(this.img, this.frame * this.size, this.type * this.size, this.size, this.size, this.origin.x - offset, this.origin.y, this.size, this.size);
    }
}*/